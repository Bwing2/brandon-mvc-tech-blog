<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./style.css">
    <title>Tech Blog</title>
</head>
<body>
    <header>
        <section class="postHeader">
            {{#if logged_in}}
            <a href="/">Home</a>
            <a href="/">Dashboard</a>
            <a href="/">Logout</a>
            {{else}}
            <a href="/">Home</a>
            <a href="/">Dashboard</a>
            <a href="/">Login</a>
            {{/if}}
        </section>
    </header>

    <main>
        {{{body}}}
    </main>

    <!-- add logged in javascript link here -->
</body>


</html>