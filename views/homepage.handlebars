{{#each posts as |post|}}

<section>
    <div class="postsHomepage">
        <h2>{{post.title}}</h2>
        <p>Created by {{post.users.name}} on {{format_date post.date_created}}</p>
        <div>
            <p>{{post.post_content}}</p>
        </div>
    </div>
</section>

{{/each}}